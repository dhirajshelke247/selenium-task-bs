{
  "name": "selenium-js-pom-framework",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha tests/*.test.js --timeout 30000 --reporter mocha-allure-reporter",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "chai": "^4.3.7",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2",
    "selenium-webdriver": "^4.20.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "mocha-allure-reporter": "^1.4.0"
  }
}
